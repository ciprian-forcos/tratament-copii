# Implementation Plan

_Generated by planning mode. Updated by building mode after each task._

(Run `./loop.sh plan` to generate this plan from specs.)

---

## Status Summary

The project scaffold is complete (React + TypeScript + Vite + Tailwind CSS + PWA), but
`src/App.tsx` is a placeholder. **Zero application code exists.** Every task below is unimplemented.

---

## Group 1 — Foundation

- [x] **1.1 TypeScript data models**
  Create `src/types.ts` with interfaces: `Child`, `Medication`, `DoseConfig`
  (all four variants: `weight_divided`, `mg_per_kg`, `fixed`, `weight_threshold`),
  `ScheduleRule` (all four rule types), and `AdministeredDose`.

- [x] **1.2 Default medications data**
  Create `src/data/medications.ts` exporting the 8 default medications from the spec
  (nurofen, panadol, diclofenac, novocalmin, vitamina_d, vitamina_c, virodep, greentus)
  with exact ids, names, doseType, doseConfig, color, and notes.

- [x] **1.3 Default schedule rules data**
  Create `src/data/scheduleRules.ts` exporting the 9 default rules (r1–r9) from
  `scheduling.md` with correct types and medication references.

- [ ] **1.4 Dose calculation logic**
  Create `src/utils/doseCalculation.ts` implementing:
  - `calculateDose(medication, weightKg, heightCm?)` — dispatches to each dose type
  - `estimateHeight(weightKg)` — lookup table from `medications.md`
  - `formatDose(amount, unit)` — returns human-readable string or "Sub doza minima"

- [ ] **1.5 LocalStorage persistence**
  Create `src/hooks/useLocalStorage.ts` — generic hook `useLocalStorage<T>(key, defaultValue)`.
  Keys to use:
  - `tratament-copii-children`
  - `tratament-copii-medications`
  - `tratament-copii-schedule-rules`
  - `tratament-copii-active-child`
  - `tratament-copii-administered-doses`

---

## Group 2 — App Shell & Navigation

- [ ] **2.1 App shell with tabs**
  Replace placeholder `src/App.tsx` with a full shell containing three bottom-navigation
  tabs: **Program**, **Medicamente**, **Copii**. Wire up state from localStorage hooks.
  Include active-child selector (name badge) in the top bar.

---

## Group 3 — Copii Tab

- [ ] **3.1 Child list UI**
  Render child profile cards showing name, weight (kg), height (cm if set), and color badge.
  Show an "Adaugă copil" button.

- [ ] **3.2 Add/Edit child dialog**
  Form dialog with fields: name (required), weight in kg (required), height in cm (optional).
  Validates that weight > 0. On save, generates a UUID id.

- [ ] **3.3 Delete child with confirmation**
  Delete button on each card; confirm before removing. If deleted child was active, switch
  active child to first remaining (or null).

- [ ] **3.4 Per-child medication toggles**
  Inside each child card (or a collapsible section), list all medications with a toggle
  switch. Toggled state stored in `child.enabledMedications`. Defaults to all enabled for
  new children.

- [ ] **3.5 Active child selection**
  Tapping/clicking a child card marks it as the active child (persisted to localStorage).
  Active child is highlighted visually.

---

## Group 4 — Medicamente Tab

- [ ] **4.1 Medication list UI**
  List all medications as cards showing: color badge, name, calculated dose for the
  active child (weight-based), unit, and notes. Show "Selectează un copil" message if no
  active child.

- [ ] **4.2 Add medication dialog**
  Form dialog with fields: name, doseType selector, doseConfig fields (dynamic per type),
  color picker (hex input), notes. On save, generates a UUID id and appends to the list.

- [ ] **4.3 Edit medication dialog**
  Same form as add, pre-populated with existing values. Saves in-place.

- [ ] **4.4 Delete medication with confirmation**
  Delete button on each card with a confirm step. Also removes the medication id from all
  children's `enabledMedications` and from all schedule rules referencing it.

- [ ] **4.5 No-duplicate name validation** _(spec: no-duplicate-medications.md)_
  Real-time check as user types the name field:
  - Case-insensitive, whitespace-trimmed comparison against all existing medication names
  - Show inline error: `"Există deja un medicament cu acest nume."`
  - Disable the "Salvează" button while error is present
  - When editing, exclude the current medication from the duplicate check

---

## Group 5 — Program Tab

- [ ] **5.1 Treatment start time picker**
  Date + time input (or datetime-local) at the top of the Program tab. Defaults to now.
  Persisted to localStorage.

- [ ] **5.2 24-hour dose timeline**
  Compute all dose times for the next 24 hours based on the active schedule rules and
  the active child's enabled medications. Display as a chronological list. Each entry
  shows: time (HH:mm), color-coded medication name badge, dose amount string.

- [ ] **5.3 Schedule rule engine**
  Create `src/utils/scheduleEngine.ts` implementing all four rule types:
  - `every_n_hours`: repeat from start time, capped at 24h window
  - `after_medication`: N hours after the most recent dose of the referenced medication
  - `once_per_day`: single occurrence at start time (or fixed time of day)
  - `times_per_day`: N evenly-spaced occurrences across 24h

- [ ] **5.4 Past / current / upcoming visual distinction**
  Doses in the past are dimmed / struck through. The next upcoming dose is highlighted.
  Doses within the next 30 minutes get a "soon" indicator.

- [ ] **5.5 Mark dose as administered**
  Checkbox or tap on a dose entry marks it as administered. Administered status stored in
  `tratament-copii-administered-doses` (keyed by child id + medication id + scheduled
  datetime). Administered doses show a check mark and are visually complete.

- [ ] **5.6 Child selector in Program tab**
  If more than one child exists, show a segmented control / tab strip at the top to switch
  the active child without leaving the Program tab.

- [ ] **5.7 Schedule rule management UI**
  A collapsible or modal "Reguli" section listing all schedule rules. Each row shows rule
  type, medication name, and parameters. Buttons to add, edit, and delete rules.

- [ ] **5.8 Add/Edit schedule rule dialog**
  Form dialog: rule type selector (every_n_hours | after_medication | once_per_day |
  times_per_day), medication selector, and type-specific parameter fields (interval hours,
  reference medication, times per day, etc.).

---

## Group 6 — UI Polish

- [ ] **6.1 Color-coded medication badges**
  Use each medication's `color` hex value for badge backgrounds throughout the app
  (timeline, medication cards, child toggles). Ensure readable contrast (white or dark text).

- [ ] **6.2 Mobile-first responsive layout**
  Max-width container (e.g., 480px) centred on desktop. Bottom nav bar stays fixed.
  All dialogs use full-screen sheet on mobile.

- [ ] **6.3 Empty states**
  Meaningful empty-state messages when:
  - No children added yet (Copii tab)
  - No active child selected (Program and Medicamente tabs)
  - No medications for the active child's schedule (Program tab)

- [ ] **6.4 Loading / hydration state**
  Show a brief loading indicator while localStorage data is being hydrated to avoid
  flash of empty content.

---

## Group 7 — Extra Features

- [ ] **7.1 BSA / height estimation display**
  In the Medicamente tab, below the active child's info, optionally show the estimated
  or entered height and the BSA (Mosteller) value used in dose calculations.

- [ ] **7.2 Service worker cache version bump**
  When the build produces a new bundle hash, update `sw.js` cache name (or automate via
  Vite plugin) so returning users get fresh assets.
