---
phase: v1-phase-07-home-next-dose
plan: 07-01
type: execute
branch: V1/phase-07-home-next-dose
---

<objective>
Show a real next-dose countdown on Home after treatment has been recorded.
Reuse `buildPlan()` — do not invent a second timing policy.
</objective>

<execution_context>
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@src/components/design/HomeB.tsx
@src/components/design/FlowProtoB.tsx
@src/components/design/dosePlan.ts
@src/components/design/doseStore.ts
@knowledge/bugs/phantom-countdown-before-treatment.md
@knowledge/medical/treatment-plan-rules.md
</context>

<tdd_rule>
For every behavior-changing task below, first commit a failing focused test as
`[test] ...`, then commit the minimum implementation as `[feat] ...`. Do not
combine test and implementation commits.
</tdd_rule>

<tasks>

<task type="auto">
  <name>Task 1: Pure next-dose from history</name>
  <files>
  src/components/design/nextPlannedDose.ts,
  src/components/design/nextPlannedDose.test.ts
  </files>
  <action>
  Add `nextPlannedDose({ child, now, doses })`. No doses for this child →
  `null`. Otherwise take the latest administered dose for that child and
  return `buildPlan({ lastMedId, lastAt }).now` (med, time). Ignore other
  children's records.
  </action>
  <verify>
  Pure tests cover empty history, other-child isolation, Nurofen→Panadol +4h,
  Panadol→Nurofen +4h, most-recent dose, and clamp-to-now when the floor has
  elapsed.
  </verify>
  <done>
  Next dose is a function of recorded history and the existing planner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Home derives next dose</name>
  <files>
  src/components/design/HomeB.tsx,
  src/components/design/HomeB.test.tsx,
  src/components/design/FlowProtoB.tsx
  </files>
  <action>
  When `nextDose` is omitted, Home derives it from `doseStore` via
  `nextPlannedDose`. Keep an explicit `nextDose` prop as a test override.
  If the derived time is still ahead, show `mai sunt …`. If it is due now,
  show `dă {med} acum` instead of `mai sunt 0m`. Remove the stale PlanCard
  persist TODO in FlowProtoB (PlanCard already records).
  </action>
  <verify>
  HomeB tests: no countdown without history; after a Nurofen record the
  Panadol countdown appears; another child's dose does not create one;
  an elapsed 4h floor shows `dă … acum`.
  </verify>
  <done>
  Returning Home after `Am dat doza` shows the real next dose.
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- nextPlannedDose`
- [ ] `npm run test -- HomeB`
- [ ] `npm run type-check`
- [ ] `npm run lint`
- [ ] `npm run test`
- [ ] `npm run build`
</verification>
