---
phase: v1-phase-08-treatment-flow
plan: 08-01
type: execute
branch: V1/phase-08-treatment-flow
---

<objective>
Complete the fever treatment episode so the second (and later) dose is a
continuation, not a new questionnaire.
</objective>

<tdd_rule>
Failing tests first, then the minimum implementation. Separate commits.
</tdd_rule>

<tasks>

<task type="auto">
  <name>Task 1: Episode last-dose helper</name>
  <files>
  src/components/design/episode.ts,
  src/components/design/episode.test.ts,
  src/components/design/nextPlannedDose.ts,
  src/components/design/nextPlannedDose.test.ts
  </files>
  <action>
  Add `lastDoseInEpisode`, `seedFromStep2`, and `alreadyRecorded`. Latest
  stored dose for the child, optionally seeded by Step 2, wins. Older than
  24h → no episode. `nextPlannedDose` uses this helper.
  </action>
</task>

<task type="auto">
  <name>Task 2: Plan card continues from history</name>
  <files>
  src/components/design/PlanCard.tsx,
  src/components/design/PlanCard.test.tsx
  </files>
  <action>
  Build the plan from episode history (Step 2 is only a seed). Persist a
  Step 2 last dose when recording or waiting. When the next dose is still
  deferred, show `Voi aștepta` and call `onWait`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Home continue vs start</name>
  <files>
  src/components/design/FlowProtoB.tsx,
  src/components/design/FlowProtoB.test.tsx
  </files>
  <action>
  If Home has a next dose, the CTA opens the plan card. Otherwise it starts
  the wizard and resets Step 2. Back from a continued plan returns Home;
  back from a first-start plan returns Step 2.
  </action>
</task>

</tasks>
