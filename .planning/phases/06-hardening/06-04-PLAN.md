---
phase: 06-hardening
plan: 06-04
type: execute
wave: B
bug: "#2, #3, #4a, #5"
---

<objective>
Fix the calm landing surface (`HomeB`): no phantom countdown before any
treatment exists, center the timeline on "now", make the temperature control
actually set the value (plus expose age + child controls), and correct one copy
string.

Output: an updated `HomeB.tsx` (and a small age control if needed), plus tests.
</objective>

<execution_context>
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@src/components/design/HomeB.tsx
@src/components/design/TempWheel.tsx
@src/components/design/childStore.ts
</context>

<reality_notes>
Verified against the current checkout (2026-06-07):
- **#2 phantom countdown:** `HomeB.tsx:28`
  `const next = nextDose ?? { at: new Date(now.getTime() + 2 * 3600_000), med: 'Panadol' }`
  invents a fake next dose when none exists; the `mai sunt {…}` line renders at
  `HomeB.tsx:59`. The line must show only when a real next dose exists
  (`nextDose` truthy).
- **#3 timeline anchor:** the strip math is **inline in HomeB** — `toPct` at
  `HomeB.tsx:38` (`(d - stripStart) / (12 * 3600_000)`) and the tick generator
  `mk` at `HomeB.tsx:193` (`now - hOffset * 3600_000`). There is **no**
  `useNightTimeline.ts` (the loose plan was wrong). Re-anchor the visible window
  so **now sits in the middle** of the strip, not pinned to one edge.
- **#4a controls:** the header temperature affordance currently does not open a
  setter. Wire it to `TempWheel.tsx` so tapping it sets the value. Expose age and
  child-selector controls (childStore provides children/active child).
- **#5 copy:** `HomeB.tsx:76` `măsoară din nou` → `Temperatura`.
</reality_notes>

<tasks>

<task type="auto">
  <name>Task 1: Failing tests for the home surface</name>
  <files>src/components/design/HomeB.test.tsx</files>
  <action>
  - #2: with `nextDose = null`, the `mai sunt …` countdown line is **not**
    rendered; with a real `nextDose`, it is.
  - #3: given `now`, the rendered position of "now" on the strip is ~50%
    (centered), within a small tolerance — not near 0% or 100%.
  - #4a: tapping the temperature control opens the setter and a selected value
    updates the displayed temperature; age + child controls are present.
  - #5: the button reads "Temperatura", not "măsoară din nou".
  Tests fail first.
  </action>
  <verify>Tests fail for the right reasons.</verify>
  <done>Failing suite encodes all four home-surface fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the fixes</name>
  <files>src/components/design/HomeB.tsx</files>
  <action>
  1. #2: remove the phantom default at line 28; render the next-dose line only
     when `nextDose` is truthy.
  2. #3: rework the window anchor so the visible strip is centered on `now`
     (adjust `stripStart`/`toPct`/`mk` so `now` maps to ~the midpoint).
  3. #4a: wire the temperature control to `TempWheel` (tap → open → set →
     reflected value). Add age + child-selector controls in the header (reuse
     `childStore` for the child list/active child).
  4. #5: change `măsoară din nou` → `Temperatura`.
  Avoid: changing the dose-rule logic (06-01) or Step 2 (06-02/06-03); keep all
  three protected localStorage keys untouched.
  </action>
  <verify>Task 1 tests pass.</verify>
  <done>Calm landing behaves correctly.</done>
</task>

</tasks>

<verification>
- [ ] No phantom `nextDose ?? {…}` default remains; countdown is conditional.
- [ ] "now" renders centered on the timeline.
- [ ] Temperature control sets the value; age + child controls present.
- [ ] Copy reads "Temperatura".
- [ ] Gate (type-check, lint, test, build) all pass; protected keys untouched.
- [ ] TDD visible in git history.
</verification>

<success_criteria>
- The first calm screen shows no fake countdown and a now-centered timeline,
  and the temperature control actually works.
</success_criteria>

<output>
After Tier-2 APPROVE, create `.planning/phases/06-hardening/06-04-SUMMARY.md`.
</output>
