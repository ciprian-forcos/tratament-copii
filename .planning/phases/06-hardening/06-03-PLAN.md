---
phase: 06-hardening
plan: 06-03
type: execute
wave: C
bug: "#10"
depends_on: ["06-01", "06-02"]
---

<objective>
Replace Step 2's last-dose preset buttons + dead `'alt...'` option with a real
date + time picker that captures the **actual datetime of the last dose**
(possibly a prior day), and make the treatment plan compute forward from that
absolute datetime. A parent may arrive days into treatment — the current
"today-only HH:MM" assumption is wrong.

Output: a new `DateTimeWheel.tsx`, an updated `Step2.tsx` that produces an
absolute datetime, a `dosePlan.ts` that consumes an absolute last-dose `Date`,
and tests.
</objective>

<execution_context>
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@src/components/design/Step2.tsx
@src/components/design/TempWheel.tsx
@src/components/design/dosePlan.ts
@src/components/design/PlanCard.tsx
</context>

<reality_notes>
Verified against the current checkout (2026-06-07):
- `Step2.tsx:23` `const TIMES = ['00:00','01:00','02:00','03:00','alt...']` —
  preset buttons; `'alt...'` is a dead control (selecting it sets
  `time='alt...'`, which downstream parsing rejects).
- `Step2Value` (`Step2.tsx:10-14`) is `{ kind, med?, time?: string }` — `time`
  is an HH:MM string.
- `PlanCard.tsx:17-22` calls `buildPlan({ ..., lastMedId, lastAtHHMM: step2.time })`.
- `dosePlan.ts:52-59` parses `lastAtHHMM` as **today's** HH:MM and rolls back a
  day only if the time is in the future — it cannot represent "two days ago".
- `TempWheel.tsx` is the existing wheel-picker pattern to reuse for the time wheel.
- Build this AFTER 06-01 (rules-driven spacing) and 06-02 (same file, Step2.tsx)
  to avoid a merge conflict and to compute forward with the correct rules.
</reality_notes>

<tasks>

<task type="auto">
  <name>Task 1: Failing tests — absolute datetime end to end</name>
  <files>src/components/design/DateTimeWheel.test.ts, src/components/design/dosePlan.test.ts</files>
  <action>
  - DateTimeWheel: emits an absolute `Date` for a chosen date + time; defaults
    to "now" on open; supports picking a prior day.
  - dosePlan: given a last dose 26h ago (absolute `Date`), the next dose is
    computed from that instant using the 06-01 rules (multi-day honored), not
    re-anchored to today.
  - No `'alt...'` value path remains.
  Tests fail first.
  </action>
  <verify>Tests fail for the right reason.</verify>
  <done>Failing suite defines the absolute-datetime contract.</done>
</task>

<task type="auto">
  <name>Task 2: Build DateTimeWheel</name>
  <files>src/components/design/DateTimeWheel.tsx</files>
  <action>
  A date + time picker reusing `TempWheel.tsx` interaction patterns: a time
  wheel (HH:MM) plus a simple date stepper/calendar (today / −1 day / −2 days
  is enough for the 3 AM case, but allow an explicit date). Output an absolute
  `Date`. Keep it calm-mode and large-tap-target consistent with the design.
  </action>
  <verify>DateTimeWheel tests pass.</verify>
  <done>Reusable absolute datetime picker exists.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Step2 + dosePlan to the absolute datetime</name>
  <files>src/components/design/Step2.tsx, src/components/design/dosePlan.ts, src/components/design/PlanCard.tsx</files>
  <action>
  - Replace the `TIMES` preset grid + `'alt...'` with `DateTimeWheel`.
  - Change `Step2Value.time` to carry an absolute value (ISO string or `Date`);
    update the `disabled`/validation logic accordingly.
  - Update `PlanCard.tsx` to pass the absolute last-dose datetime through.
  - Replace `dosePlan.ts`'s `lastAtHHMM` (today-only) handling with the absolute
    last-dose `Date`; next-dose computation uses it with the 06-01 rules.
  Avoid: changing the rules logic (owned by 06-01); reintroducing today-only
  assumptions.
  </action>
  <verify>All tests pass; no `'alt...'` and no `lastAtHHMM` today-only parsing remain.</verify>
  <done>End-to-end absolute last-dose datetime drives the plan.</done>
</task>

</tasks>

<verification>
- [ ] `grep -rn "alt\.\.\.\|lastAtHHMM" src/components/design` shows the dead path gone.
- [ ] A last dose on a prior day produces a correct forward plan.
- [ ] Gate (type-check, lint, test, build) all pass.
- [ ] TDD visible in git history.
</verification>

<success_criteria>
- Parent can set a last-dose datetime including a prior day; plan math uses it.
</success_criteria>

<output>
After Tier-2 APPROVE, create `.planning/phases/06-hardening/06-03-SUMMARY.md`.
</output>
