---
phase: 06-hardening
plan: 06-06
type: execute
wave: A
bug: "#1"
---

<objective>
Add an "Add to Home Screen" affordance so the PWA can be installed from the
phone (bug #1). Android/Chrome via the `beforeinstallprompt` event; iOS Safari
via a short instructions sheet. Don't nag, and hide it when already installed.

Output: a new `InstallPrompt.tsx`, integration into `App.tsx`/`HomeB`, a
verified `manifest.json`, and tests.
</objective>

<execution_context>
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@src/App.tsx
@src/components/design/HomeB.tsx
@public/manifest.json
</context>

<reality_notes>
Verify before editing (2026-06-07):
- Confirm the manifest path/name (`public/manifest.json` or similar) and that it
  has the fields required for installability (name, short_name, icons,
  start_url, display: standalone, theme/background color). Fix gaps if present.
- Confirm where the app root renders (`App.tsx`) to mount the prompt globally.
</reality_notes>

<tasks>

<task type="auto">
  <name>Task 1: Failing tests for the install prompt</name>
  <files>src/components/design/InstallPrompt.test.tsx</files>
  <action>
  - The "Adaugă pe ecranul principal" button appears after a
    `beforeinstallprompt` event fires (Android path); clicking it calls the
    captured event's `prompt()`.
  - In standalone mode (`matchMedia('(display-mode: standalone)')` true), the
    prompt is hidden (already installed).
  - iOS path (no `beforeinstallprompt`): an instructions sheet (Share → "Add to
    Home Screen") is shown instead.
  - Dismiss hides it for the session and it does not re-nag.
  Tests fail first.
  </action>
  <verify>Tests fail for the right reason.</verify>
  <done>Failing suite defines install-prompt behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement InstallPrompt + integrate</name>
  <files>src/components/design/InstallPrompt.tsx, src/App.tsx</files>
  <action>
  - Capture `beforeinstallprompt` (preventDefault, stash the event), surface a
    calm "Adaugă pe ecranul principal" button that triggers `prompt()`.
  - iOS Safari fallback: short instructions sheet.
  - Hide when `display-mode: standalone`; dismissible; show once (session-level
    dismissal is fine — do not add a new persisted localStorage key without
    declaring it; if persistence is wanted, use an additive key only).
  - Mount globally from `App.tsx` (or `HomeB`), consistent with the calm design.
  </action>
  <verify>Task 1 tests pass.</verify>
  <done>Install affordance works on both platforms, hidden when installed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify manifest installability</name>
  <files>public/manifest.json</files>
  <action>Ensure name, short_name, icons (≥192 & 512), start_url, scope,
  `display: standalone`, theme_color, background_color are present and correct.
  Fix any gaps. Confirm the service worker still registers (don't regress it).</action>
  <verify>`npm run build` produces a manifest the browser treats as installable;
  no SW regression.</verify>
  <done>Manifest meets installability requirements.</done>
</task>

</tasks>

<verification>
- [ ] Install button appears on `beforeinstallprompt`; hidden in standalone.
- [ ] iOS instructions sheet shown when the event is unavailable.
- [ ] Manifest installability fields present; SW still registers.
- [ ] No change to the three protected localStorage keys (any new key is additive).
- [ ] Gate (type-check, lint, test, build) all pass.
</verification>

<success_criteria>
- A parent on Android gets a real install prompt; an iOS parent gets clear
  instructions; installed users see neither.
</success_criteria>

<output>
After Tier-2 APPROVE, create `.planning/phases/06-hardening/06-06-SUMMARY.md`.
</output>
