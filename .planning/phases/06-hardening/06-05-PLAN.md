---
phase: 06-hardening
plan: 06-05
type: execute
wave: A
bug: "#4b, #12, #8, #6"
---

<objective>
Remove fake chrome and out-of-scope affordances from the active V1 surfaces:
the fake status icons (#4b) and the inaccurate "112" banner (#12) are concrete
deletions; the BSA/estimated-height line (#8) and the add-medicine control (#6)
require a short locate-then-decide step because the loose plan's targets did
not match the current code.

Output: `StatusBar.tsx` without fake icons, `PlanCard.tsx` without the 112
banner, and a documented resolution for #8 and #6.
</objective>

<execution_context>
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@src/components/design/StatusBar.tsx
@src/components/design/PlanCard.tsx
@src/components/design/ChildEditor.tsx
@src/types.ts
</context>

<reality_notes>
Verified against the current checkout (2026-06-07):
- **#4b (concrete):** `StatusBar.tsx:26` renders a fake signal glyph
  (`<span>📶</span>`) and `:27` a fake `<span className="battery" />`. Remove the
  fake status icons.
- **#12 (concrete):** `PlanCard.tsx:142`
  `<span>peste 39.5°C apelează 112 / pediatru. asta e ghidaj, nu rețetă.</span>` —
  remove this banner. (Inaccurate; real guidance is ~40–41°C + tepid bath; out
  of scope to rewrite — just remove.)
- **#8 (needs locating):** there is **no BSA line and no `ChildrenScreen.tsx`**
  in the active design surfaces (Phase 4 children-screen was never built). A
  `height` field exists in `types.ts:98` ("estimated from weight if missing")
  and in the **legacy** `CopiiTab.tsx` (reference-only, must not be deleted per
  PROCESS.md). Locate what the QA tester actually saw.
- **#6 (needs locating):** `ChildEditor.tsx:142` `+ adaugă` calls
  `childStore.add()` — that is **add-child**, legitimate, not add-medicine. The
  only add-medicine affordance is in the legacy `MedicamenteTab` (reference-only,
  must not be reachable in V1, must not be deleted).
</reality_notes>

<tasks>

<task type="auto">
  <name>Task 1: Remove fake status icons (#4b)</name>
  <files>src/components/design/StatusBar.tsx</files>
  <action>Remove the fake signal/battery glyphs (lines ~26-27). Keep any real
  status content (e.g. the clock) intact. Update/remove any test that asserted
  the fake icons.</action>
  <verify>No `📶`/`battery` fake-status markup remains; gate passes.</verify>
  <done>Status bar shows no fake hardware icons.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the 112 banner (#12)</name>
  <files>src/components/design/PlanCard.tsx</files>
  <action>Remove the `peste 39.5°C apelează 112 …` banner (line ~142) and any
  now-dead wrapper/styles for it. Update/remove any test asserting it.</action>
  <verify>`grep -rn "112" src/components/design` shows the banner gone; gate passes.</verify>
  <done>Inaccurate emergency banner removed.</done>
</task>

<task type="checkpoint:investigate">
  <name>Task 3: Locate and resolve #8 (BSA/height) and #6 (add-medicine)</name>
  <files>(investigation — then edit only active surfaces)</files>
  <action>
  For each, search the **active design-B surfaces** (not legacy tabs):
  - #8: find any BSA / estimated-height / m² line shown on a reachable V1
    screen. If found, remove it. If it exists only in legacy `CopiiTab.tsx`
    (reference-only, unreachable in V1), mark #8 **N/A** with file:line
    evidence — do NOT delete the legacy tab.
  - #6: confirm whether any add-medicine control is reachable in V1.
    `ChildEditor.tsx:142 "+ adaugă"` is add-child (keep). If the only add-med
    affordance is in legacy `MedicamenteTab` and is unreachable, mark #6 **N/A**
    with evidence. If a reachable add-medicine control exists on an active
    surface, remove it (keep the 4 default antipyretics; do not touch the three
    protected localStorage keys).
  Record the resolution (removed vs N/A-with-evidence) in the status report and
  the SUMMARY.
  </action>
  <verify>Each of #8 and #6 is either removed (with file:line) or N/A (with
  evidence it is unreachable in V1). No legacy reference component deleted.</verify>
  <done>#8 and #6 explicitly resolved, no scope creep into legacy code.</done>
</task>

</tasks>

<verification>
- [ ] Fake status icons gone; 112 banner gone.
- [ ] #8 and #6 each resolved (removed or N/A-with-evidence).
- [ ] Legacy tabs (`MedicamenteTab`, `CopiiTab`, `ProgramTab`) NOT deleted.
- [ ] Protected localStorage keys untouched.
- [ ] Gate (type-check, lint, test, build) all pass.
</verification>

<success_criteria>
- No fake hardware chrome, no inaccurate 112 banner on active surfaces.
- BSA line and add-medicine affordance are absent from reachable V1 surfaces.
</success_criteria>

<output>
After Tier-2 APPROVE, create `.planning/phases/06-hardening/06-05-SUMMARY.md`,
recording the #8/#6 resolution explicitly.
</output>
