---
phase: 06-hardening
plan: 06-02
type: execute
wave: B
bug: "#11, #9"
---

<objective>
Step 2 ("Ai mai dat ceva?") must offer **only antipyretics** as the last-dose
medication, and its title copy must be corrected. Bug #11: `Virodep` (vit D+C)
is not an antipyretic and must not appear. Bug #9: the title is wrong.

Output: a `Step2.tsx` whose medication list contains only antipyretics, with
corrected title copy, plus a `Step2.test.ts`.
</objective>

<execution_context>
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@src/components/design/Step2.tsx
@src/data/medications.ts
</context>

<reality_notes>
Verified against the current checkout (2026-06-07):
- `Step2.tsx:3-8` defines a local `MEDS` array with four entries:
  `nurofen`, `panadol`, `diclofenac` (labelled "Novocalmin / supozitor
  diclofenac"), and `virodep` (vit D+C). Only `virodep` is a non-antipyretic.
- Title is at `Step2.tsx:33`: `title="Ai mai dat ceva?"`.
- There is no shared "antipyretic id list" today; `MEDS` is a local literal.
</reality_notes>

<tasks>

<task type="auto">
  <name>Task 1: Failing tests for Step 2 medication list + title</name>
  <files>src/components/design/Step2.test.ts</files>
  <action>
  Render Step 2 in the "last dose" state and assert:
  - `Virodep` is **never** rendered as a selectable medication.
  - The antipyretics (Nurofen, Panadol, and the diclofenac/Novocalmin entry)
    are present.
  - The step title reads **"Ai mai administrat altceva?"** (bug #9).
  Tests fail first against the current code.
  </action>
  <verify>Tests fail (virodep still present; old title).</verify>
  <done>Failing suite encodes the antipyretics-only + copy fix.</done>
</task>

<task type="auto">
  <name>Task 2: Antipyretics only + title fix</name>
  <files>src/components/design/Step2.tsx</files>
  <action>
  - Remove the `virodep` entry from `MEDS`. Prefer deriving the list from a
    single shared antipyretic id source (e.g. a small exported constant in
    `medications.ts` or a filter on medication category) so it cannot drift;
    a corrected local literal is acceptable if a shared source is out of scope.
  - Change the title at line 33 to **"Ai mai administrat altceva?"**.
  Avoid: touching the time-entry controls (that is 06-03's scope); changing
  `Step2Value`'s shape.
  </action>
  <verify>Task 1 tests pass.</verify>
  <done>Step 2 shows only antipyretics; title corrected.</done>
</task>

</tasks>

<verification>
- [ ] `grep -n virodep src/components/design/Step2.tsx` returns nothing.
- [ ] Title string is "Ai mai administrat altceva?".
- [ ] Gate (type-check, lint, test, build) all pass.
- [ ] TDD visible in git history.
</verification>

<success_criteria>
- A parent can never pick a non-antipyretic as the last dose.
- Title copy is correct.
</success_criteria>

<output>
After Tier-2 APPROVE, create `.planning/phases/06-hardening/06-02-SUMMARY.md`.
</output>
