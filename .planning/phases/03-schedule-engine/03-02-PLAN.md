---
phase: 03-schedule-engine
plan: 03-02
type: execute
---

<objective>
Implement the adapter approved in `03-01` and use it in `buildPlan()`. Drop
the hardcoded `+2h` math. `buildPlan()` now computes the next dose time
through the real schedule engine.

Purpose: Without this, the panic flow gives parents wrong "next dose" times.
Output: A `scheduleAdapter` module (tests first) + `buildPlan()` rewritten
to use it.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/03-schedule-engine/FINDINGS.md
@src/components/design/dosePlan.ts
@src/components/design/dosePlan.test.ts
@src/utils/scheduleEngine.ts
@src/data/scheduleRules.ts
@src/data/medications.ts
@src/components/design/doseStore.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for scheduleAdapter</name>
  <files>src/components/design/scheduleAdapter.test.ts</files>
  <action>
  Following the API agreed in FINDINGS.md, write tests against the not-yet-
  existing `scheduleAdapter`. The tests must fail because the module is
  missing.

  Cases at minimum (refine per FINDINGS):
  - Given no administered doses, `nextDoseFor({ medicationId: 'nurofen',
    childId, now })` returns a `Date` equal to `now` (eligible immediately).
  - Given an administered Nurofen 30 min ago, calling for Nurofen returns
    `lastAt + 8h` (per Nurofen's "every 8 hours" rule in the seed data —
    confirm exact figure from `scheduleRules.ts`).
  - Given an administered Nurofen 30 min ago, calling for Panadol returns
    a time at least 2h from now (the min-spacing rule between Ibuprofen and
    Paracetamol).
  - Unknown medicationId returns `null`.
  - Fake-timer everything — no real wall clock.
  </action>
  <verify>
  Tests fail with the missing-module error.
  </verify>
  <done>
  Failing test suite present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement scheduleAdapter</name>
  <files>src/components/design/scheduleAdapter.ts</files>
  <action>
  Implement the adapter per FINDINGS.md. Pull administered doses from
  `doseStore.listFor(childId)`, medications from `DEFAULT_MEDICATIONS`, and
  rules from wherever `scheduleRules.ts` exports them.

  Specific guardrails:
  - Pure function modulo `doseStore` reads. No side effects.
  - Accepts `now: Date` as an explicit argument. Never call `new Date()`
    inside.
  - Returns `Date` for `at`, never strings.
  - Defensive against an empty rules set: if no rule applies, return `now`
    (eligible). Document this in the tests.

  Avoid: re-implementing parts of `scheduleEngine.ts` inside the adapter.
  The adapter is a thin translation layer.
  </action>
  <verify>
  All scheduleAdapter tests pass. Other tests still pass.
  </verify>
  <done>
  Adapter green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite buildPlan() to use the adapter + update tests</name>
  <files>
  src/components/design/dosePlan.ts,
  src/components/design/dosePlan.test.ts
  </files>
  <action>
  TDD:
  1. If `dosePlan.test.ts` does not exist (Phase 1 may have skipped it),
     create it now with these cases:
     - First treatment, no history → "now" is Nurofen at `now`, "next" is
       Panadol at the engine-computed time.
     - Last given Nurofen at 03:00 → "now" is Panadol; "next" is Nurofen
       at the engine-computed time.
     - Last given Panadol at 03:00 → "now" is Nurofen; "next" is Panadol
       at the engine-computed time.
     - Sub-dose case: when `calculateDose` returns `'sub_doza'`, the plan
       still returns a step but `amount` is `'sub_doza'` (existing behavior
       — protect it with a test).
     Mock `doseStore` and the schedule engine via dependency injection or
     `vi.mock`. Document which.
  2. Run — tests fail because `buildPlan` still uses `+2h` for the "next"
     timing.
  3. Modify `buildPlan` to call `scheduleAdapter.nextDoseFor(...)` for the
     `next` step instead of computing `+2 * 3600_000`. Keep the alternation
     policy (Nurofen ↔ Panadol) where it is.
  4. The "now" step keeps its current behavior (immediate or `lastAt + 2h`
     for explicit last-dose times entered in Step 2). Don't break that.
  5. Tests green.

  Avoid: changing the public shape of `Plan` or `PlannedStep`. PlanCard's
  rendering must continue to work without changes.
  </action>
  <verify>
  - All dosePlan tests pass.
  - Existing PlanCard test still passes (no regression).
  - `npm run type-check`, `npm run build` pass.
  - Manual: at 3 AM mock-time, "next" timing matches what the engine
    computes from real rules.
  </verify>
  <done>
  No more `+2 * 3600_000` hardcode in `dosePlan.ts`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Real schedule engine drives the "next dose at" time in the plan card.
  </what-built>
  <how-to-verify>
  1. `git checkout phase-03/03-02-schedule-engine && npm install && npm run dev`
  2. Walk through the panic flow with "Primul tratament" → check the plan
     card's "apoi · HH:MM (în Xh)" line is consistent with Nurofen's rule.
  3. Tap "Am dat doza", walk through again with "Ultima doză a fost"
     → Nurofen at 03:00 → confirm the "next" timing reflects the engine,
     not a flat 2h offset.
  4. Edit `src/data/scheduleRules.ts` locally to set Nurofen's interval to
     7 hours (revert after). Restart dev server. Confirm "next" timing
     shifts. Revert.
  </how-to-verify>
  <resume-signal>
  Reply "approved" or comment.
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] `grep -n "2 \* 3600" src/components/design/dosePlan.ts` returns nothing.
- [ ] All tests pass.
- [ ] `npm run type-check` and `npm run build` pass.
</verification>

<success_criteria>
- Hardcode removed.
- Adapter has dedicated tests.
- `buildPlan` tests cover first-treatment and alternation cases.
</success_criteria>

<output>
After merge, create `.planning/phases/03-schedule-engine/03-02-SUMMARY.md`.
</output>
