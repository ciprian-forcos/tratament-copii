---
phase: 00-baseline
plan: 00-01
type: execute
---

<objective>
Bring the uncommitted design B implementation into git on a feature branch and
open a PR. This is the foundation V1 builds on; nothing else moves until it's
merged to `main`.

Purpose: Lock in the design B GUI as the new app shell so the V1 phases have a
stable base to build against.
Output: One PR titled `[phase-00] design B baseline` against `main`.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@.planning/PROCESS.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/PROCESS.md
@src/App.tsx
@src/index.css
@src/types.ts
@src/components/design/FlowProtoB.tsx
@src/components/design/HomeB.tsx
@src/components/design/PlanCard.tsx
@src/components/design/Step1.tsx
@src/components/design/Step2.tsx
@src/components/design/StepShell.tsx
@src/components/design/ChildPill.tsx
@src/components/design/ChildEditor.tsx
@src/components/design/TempWheel.tsx
@src/components/design/AnalogClock.tsx
@src/components/design/StatusBar.tsx
@src/components/design/childStore.ts
@src/components/design/dosePlan.ts
@index.html
@sw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the working tree builds before committing</name>
  <files>(no edits — verification only)</files>
  <action>
  Run `npm install && npm run type-check && npm run build` on the current
  working tree. All three must succeed. If any fail, STOP — do not commit a
  broken baseline. Report the failure to the reviewer in the PR description
  instead of opening the PR.

  Avoid: running `npm audit fix` or `npm update` — they will churn the lockfile
  and aren't in scope.
  </action>
  <verify>
  Exit code 0 from each command. `dist/` produced by the build.
  </verify>
  <done>
  All three commands pass with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the design B work to a feature branch</name>
  <files>
  index.html, sw.js, src/App.tsx, src/index.css, src/types.ts,
  src/components/design/*.tsx, src/components/design/*.ts
  </files>
  <action>
  - Create branch: `phase-00/00-01-design-b-baseline` from `main`.
  - `git add` ONLY the files listed in `<files>` above. Do NOT `git add -A` —
    that can sweep in editor cruft. Verify the staged set with `git status`
    before committing.
  - Make ONE commit with this message:
    ```
    [design] add design B GUI shell

    Implements the saved design B from claude.ai/design as the new app shell.

    - Home B screen with analog clock + night timeline + child pill
    - 2-step flow (temperature → first/last treatment) → plan card
    - Child editor and temperature wheel as bottom sheets
    - Multi-child reactive store backed by existing localStorage keys
    - Dark-mode triage style with Caveat / Inter / JetBrains Mono

    Old tabs (Program/Medicamente/Copii) remain in the tree but are not
    reachable from the new flow yet. They will be surfaced again from the
    ≡ menu in phase 04.

    Plan: .planning/phases/00-baseline/00-01-PLAN.md
    ```
  - Push the branch to `origin`.

  Avoid: amending an existing commit; force-pushing; signing off with
  `--no-verify`.
  </action>
  <verify>
  - `git log --oneline -1` shows the new commit on the feature branch.
  - `git diff main..HEAD --stat` shows ONLY the files listed in `<files>`.
  - `git push -u origin phase-00/00-01-design-b-baseline` succeeds.
  </verify>
  <done>
  Branch pushed; commit history clean; no stray files in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Open the PR</name>
  <files>(GitHub side, no local edits)</files>
  <action>
  Use the GitHub CLI:
  ```
  gh pr create --base main --head phase-00/00-01-design-b-baseline \
    --title "[phase-00] design B baseline" \
    --body-file <(echo ...)
  ```
  Fill the body using the PR template in `.planning/PROCESS.md`. Specifically:
  - Plan: `.planning/phases/00-baseline/00-01-PLAN.md`
  - Summary: brief description of design B (3 bullets max)
  - Files changed: list from `git diff main..HEAD --name-status`
  - Tests: "No new tests in this plan — test infra arrives in 00-02."
  - Gate: all four boxes checked (type-check, lint, test if available, build).
    If `npm test` doesn't exist yet, write
    `npm run test (not installed yet — added in plan 00-02)` for that line.
  - Manual smoke check: "ran `npm run dev`, clicked through Home → Step 1 →
    Step 2 → Plan card → Am dat doza returns home."
  - Deviations: "None."

  Avoid: marking the PR as a draft; assigning reviewers other than the user.
  </action>
  <verify>
  `gh pr view` shows the PR open against `main` with the correct title and
  the PR body matches the template structure.
  </verify>
  <done>
  PR URL printed for the reviewer.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  The design B GUI is committed and a PR is open. The reviewer should pull
  the branch, run it locally, and confirm the panic flow works end-to-end.
  </what-built>
  <how-to-verify>
  1. `git fetch && git checkout phase-00/00-01-design-b-baseline`
  2. `npm install && npm run dev`
  3. Open http://localhost:5173/tratament-copii/
  4. Click through: Home B → "Următoarea doză" → Step 1 (adjust temp) →
     Step 2 (pick "Primul tratament") → Plan card → "Am dat doza" returns
     home.
  5. Tap the child pill — Child editor sheet opens. Switch to Luca, switch
     back to Maya. Close.
  6. Tap "măsoară din nou" — Temp wheel opens. Change to 39.5, save.
     Confirm the chip and Step 1 reflect the new value.
  7. Reload — values persisted.
  </how-to-verify>
  <resume-signal>
  Reply "approved" to merge, or list issues to fix on the branch.
  </resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] PR open, reviewer approved, branch merged to `main` via squash.
- [ ] Feature branch deleted after merge.
- [ ] `git log main --oneline -1` shows the squashed `[phase-00] ...` commit.
- [ ] `npm run build` still passes on `main`.
</verification>

<success_criteria>
- Design B implementation is on `main`.
- No data loss for users coming from the previous `main` (localStorage keys
  preserved).
- No regressions in `npm run build`.
</success_criteria>

<output>
After merge, create `.planning/phases/00-baseline/00-01-SUMMARY.md` with:

# Phase 0 Plan 1: Design B baseline — Summary

**Design B GUI shell is on `main` and the panic flow works end-to-end.**

## Accomplishments
- Committed N files of design B implementation
- PR #X merged via squash

## Files Created/Modified
[list from `git diff` at PR time]

## Decisions Made
None — this plan only commits prior work.

## Issues Encountered
[Anything that came up during the smoke check]

## Next Step
Ready for `00-02-PLAN.md` (test infrastructure).
</output>
