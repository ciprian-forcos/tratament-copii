---
phase: 04-children-screen
plan: 04-01
type: execute
---

<objective>
Make the ≡ MenuBtn on HomeB open a calm-mode "Copii" screen that lists all
children with quick actions (edit / set active / delete), and from which the
existing `ChildEditor` bottom sheet can be opened for a chosen child. The
screen also exposes a "Înapoi" back button to HomeB. Surface the existing
`CopiiTab` content (height/BSA, enabled medications, etc.) as a read-only
section per child.

Purpose: V1 needs a calm-mode entry point that isn't the panic flow.
Output: A new `ChildrenScreen` component reachable from HomeB.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@.planning/PROCESS.md
@.planning/TEST_CONVENTIONS.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@src/components/design/HomeB.tsx
@src/components/design/ChildPill.tsx
@src/components/design/ChildEditor.tsx
@src/components/design/childStore.ts
@src/components/design/FlowProtoB.tsx
@src/components/CopiiTab.tsx
@src/utils/doseCalculation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing ChildrenScreen test</name>
  <files>src/components/design/ChildrenScreen.test.tsx</files>
  <action>
  Test what the screen does, not how it looks:
  - Renders all children from `useChildren()`.
  - Shows the active child distinctly (e.g. an "activ" marker — assert via
    text or aria-current).
  - Tapping "Editează" on a child opens the `ChildEditor` sheet.
  - Tapping "Setează activ" on a non-active child changes the store's
    activeId.
  - Tapping the "+ Adaugă" button calls `childStore.add()`.
  - Tapping "Înapoi" calls the provided `onBack` callback.
  - For each child, the BSA + estimated height (from `estimateHeight()` and
    `calculateBSA()` in `src/utils/doseCalculation.ts`) is rendered.
  - Lists the medications enabled for the active child by name.

  Tests must fail because `ChildrenScreen.tsx` does not exist.

  Avoid: testing the layout of the panel. Just behavior.
  </action>
  <verify>
  Tests fail with module-not-found.
  </verify>
  <done>
  Failing tests in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ChildrenScreen</name>
  <files>src/components/design/ChildrenScreen.tsx</files>
  <action>
  Build a screen component matching the design B visual language:
  - Wrapped in `<div className="phone">`, with `StatusBar` and a back chevron
    in the top bar.
  - "Copii" eyebrow / title.
  - List of children, each showing: avatar bubble (initial), name, age in
    words (`ageWords()`), weight, BSA / height estimate. Active child is
    bordered with `--accent`.
  - Per-row actions: "Editează" (opens `ChildEditor` for that child),
    "Setează activ" (only on non-active rows).
  - Section "Medicamente active pentru {activeName}": a read-only list of
    medication names from `enabledMedications` resolved via
    `DEFAULT_MEDICATIONS`. Each row shows name and dose-type label.
  - Footer: "+ Adaugă copil" button + "Înapoi" button (calls `onBack` prop).

  Props:
  ```ts
  interface ChildrenScreenProps {
    onBack: () => void
  }
  ```

  Use existing design tokens (`var(--bg-2)`, `var(--accent)` etc.). Match the
  spacing / radius vocabulary already in `HomeB`.

  Reuse `ChildEditor` for the edit affordance — do not reinvent fields.

  Avoid: importing anything from `src/components/CopiiTab.tsx`. That file
  uses Tailwind classes that don't match design B's CSS. Translate the
  relevant logic instead.
  </action>
  <verify>
  - Component tests pass.
  - `npm run type-check`, `npm run build` pass.
  </verify>
  <done>
  ChildrenScreen green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire ≡ MenuBtn in HomeB and route in FlowProtoB</name>
  <files>
  src/components/design/HomeB.tsx,
  src/components/design/ChildPill.tsx,
  src/components/design/FlowProtoB.tsx
  </files>
  <action>
  1. Add a `'children'` page to the `FlowProtoB` page-state union:
     ```ts
     type Page = 'home' | 's1' | 's2' | 'done' | 'children'
     ```
     Render `<ChildrenScreen onBack={() => setPage('home')} />` for
     `page === 'children'`.
  2. Add an `onMenu` prop to `MenuBtn` in `ChildPill.tsx` if not already
     present. (Spec says it accepts `onClick`; verify in the current file
     and reuse.)
  3. In `HomeB.tsx`, accept an `onMenu` prop. Pass it to `<MenuBtn
     onClick={onMenu} />`.
  4. In `FlowProtoB.tsx`, pass `onMenu={() => setPage('children')}` to
     `<HomeB />`.

  Update the `HomeB.test.tsx` test that renders the component — give it an
  `onMenu` stub.

  Avoid: removing the ≡ button or changing its visual size.
  </action>
  <verify>
  - `FlowProtoB` test (write a simple one if absent) confirms that tapping ≡
    routes to `ChildrenScreen` and "Înapoi" routes back.
  - All previous tests still pass.
  - Manual: tap ≡ on Home → ChildrenScreen opens. Tap Înapoi → back home.
  </verify>
  <done>
  ≡ menu fully wired.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Calm-mode children management screen reachable from HomeB.
  </what-built>
  <how-to-verify>
  1. Checkout the branch, run dev server.
  2. Tap ≡ on the home screen → Copii screen opens.
  3. List shows Maya + Luca (or whatever the active store has).
  4. Tap "Editează" on Luca → ChildEditor sheet opens with Luca's fields.
  5. Change Luca's weight, close → list reflects the new weight + new BSA.
  6. Tap "Setează activ" on Luca → row updates, header now shows Luca's
     temperature in the pill.
  7. Tap "+ Adaugă copil" → new child appears, ChildEditor opens to edit
     name.
  8. Tap "Înapoi" → back to HomeB. Pill shows the active child.
  </how-to-verify>
  <resume-signal>
  Reply "approved" or comment.
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] All tests pass.
- [ ] `npm run type-check`, `npm run build` pass.
- [ ] Manual flow matches the checkpoint.
</verification>

<success_criteria>
- ChildrenScreen exists and is reachable from HomeB via ≡.
- The screen lets the parent perform: list, set-active, edit, add, delete
  (delete is via `ChildEditor`'s "Şterge profilul"). No new delete UI.
</success_criteria>

<output>
After merge, create `.planning/phases/04-children-screen/04-01-SUMMARY.md`.
</output>
